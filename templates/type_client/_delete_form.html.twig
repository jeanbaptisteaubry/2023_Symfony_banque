<form method="post" action="{{ path('app_type_client_delete', {'id': type_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_client.id) }}">
    <button class="btn">Delete</button>
</form>
