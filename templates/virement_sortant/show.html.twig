{% extends 'base.html.twig' %}

{% block title %}VirementSortant{% endblock %}

{% block body %}
    <h1>VirementSortant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ virement_sortant.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ virement_sortant.date ? virement_sortant.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IbanDestinataire</th>
                <td>{{ virement_sortant.ibanDestinataire }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ virement_sortant.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_virement_sortant_index') }}">back to list</a>

    <a href="{{ path('app_virement_sortant_edit', {'id': virement_sortant.id}) }}">edit</a>

    {{ include('virement_sortant/_delete_form.html.twig') }}
{% endblock %}

{% block menu %}
    {% import 'macro/macroMenu.html.twig' as macroMenu %}
    {{ macroMenu.menu('Ma banque',7) }}
{% endblock %}