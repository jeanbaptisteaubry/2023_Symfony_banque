<form method="post" action="{{ path('app_virement_sortant_delete', {'id': virement_sortant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ virement_sortant.id) }}">
    <button class="btn">Delete</button>
</form>
